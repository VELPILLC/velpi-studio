/*!
 * Tavola — Bootstrap 6 restaurant template
 *
 * Thin by design: the tabbed menu is Bootstrap 6 tabs, the FAQ is native
 * <details>, and the booking form is data-bs-validate. Nothing here needs
 * bespoke behaviour beyond the shared base.
 */

import { initBase, onReady } from "./base.js"

onReady(() => {
  initBase()
})
